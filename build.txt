Transport Application Build and Run Instructions (from repository root)

1) Build/setup backend dependencies:
make install

2) Start the application:
make run

After step 2, open http://localhost:5000 in your browser.
